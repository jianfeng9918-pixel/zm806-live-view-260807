# 周麻婆 806 储值战报 V1｜Design QA

final result: passed

## 验收基准

- 视觉源文件：`design/reference-mobile.png`（825 × 1906）。
- 同尺寸对照源：`design/reference-mobile-393x852.png`（将源图等比缩放至393px宽，再截取顶部852px；未拉伸）。
- 总部真实手机视口：`design/implementation-real-viewport-393x852.png`（393 × 852）。
- 门店真实手机视口：`design/implementation-store-393x852-final.png`（393 × 852，累计口径）。
- 区域真实手机视口：`design/implementation-region-393x852-final.png`（393 × 852，今日口径）。
- 门店并排对照：`design/qa-comparison-store-mobile-final.png`（左为样稿、右为实现）。
- 总部风格对照：`design/qa-comparison-hq-mobile-final.png`（左为门店样稿设计语言、右为新增总部版）。
- 桌面预览中的内部设备画布也按393 × 852检查；另检查 Pixel 10 的427px内容宽度，截图为 `design/implementation-pixel427-final-stage.png`。

## 对照状态

- 样稿：福州晋安三盛广场店，累计口径，包含示意趋势和30分钟增量。
- 实现门店版：同门店、累计口径，使用BI 2026-08-06 15:18真实快照。因当前只有首个成功快照，趋势和30分钟增量显示“首轮快照已建立／待下次快照”，不伪造样稿中的示意增量。
- 实现总部版：今日口径，重点检查新增“区域今日达成率”模块。
- 实现区域版：莆田区域，分别检查今日与累计口径、区域榜单和区域内督促门店。

## 视觉与内容检查

- 字体与层级：沿用样稿的中文系统字体、红色大金额、黑色排名、三档目标和灰色辅助信息；总部、区域、门店首屏层级清晰。
- 布局与密度：393px真实浏览器宽度无横向滚动；查看范围、口径切换、金额、排名、三档目标和奖金均能在门店首屏连续阅读。
- 色彩与状态：品牌红橙、达标绿色、预警红色和中性灰与样稿一致；未增加渐变或无关装饰。
- 图片与图标：使用周麻婆真实Logo；功能图标统一使用Radix图标库，没有占位图、手绘SVG或CSS图形替代。
- 总部管理模块：已达标区域绿色、最低区域红色、差额、30分钟状态、区域完整排名及可点击跳转均已呈现。
- 文案：首个快照、数据延迟、对账异常、搜索无结果等状态均使用可理解的经营语言，没有把未知值写成0。

## 响应式与可访问性

- 393 × 852：以网页本体全宽显示，不再把手机模型嵌套进真实手机屏幕；无横向滚动。
- 427px：Pixel 10画布下金额、目标卡、督促榜和详细列表无重叠、裁切或异常换行。
- 桌面：保留手机设备预览，便于内部调试和视觉复核。
- 控件：选择框、今日／累计标签、区域／门店跳转、排行榜展开、排序和搜索均为语义化控件。
- 可访问性：Logo含替代文本；趋势、目标进度和状态有语义标签；键盘焦点有清晰红色轮廓；支持减少动态效果偏好。

## 交互与数据检查

- 总部、莆田区域、福州晋安三盛广场店三种视角切换通过；切换视角后自动回到首屏顶部。
- 今日／累计切换通过；门店今日视角仍保留累计对赌、拼搏、挑战三档达成率和奖金。
- 搜索“莆田学园”只返回“莆田学园南路店”；清空后恢复区域全部门店。
- 131家门店、13个区域完整进入选择框和列表；总部当前为2个今日达标区域，最低为莆田区域。
- 浏览器控制台警告和错误：0。
- 数据规则测试通过：无区域达标、多个区域达标、目标为零、并列最低、更新失败保留上一版。

## 修正记录

1. P1 响应式：真实393px窗口最初会把设备外框整体缩小，导致正文过小；已在窄屏改为网页全宽直出，桌面仍保留设备预览。
2. P2 内容完整性：门店“今日”口径最初只显示今日挑战目标；已固定保留对赌、拼搏、挑战和奖金，并额外显示今日目标。
3. P2 交互：从长列表切换区域／门店时最初会继承旧滚动位置；已在视角切换时回到顶部。
4. P2 区域督促：区域内不足3个总部摘要门店时，已改为从该区域门店按规则补足，不再混入其他区域。
5. P2 可访问性：补充键盘焦点状态并关闭趋势图非必要动画。

## 已知且符合预期的状态

- 首个快照无法计算30分钟变化，页面显示等待下一次更新；第二次成功快照且间隔20–40分钟后才生成增量和趋势。
- 页面关闭普通文本选择、右键和导出入口，但公开网页无法真正阻止截图、开发者工具读取或数据转存。
