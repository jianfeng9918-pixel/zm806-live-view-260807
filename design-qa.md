# 周麻婆 806 储值战报 V1.1｜Design QA

final result: passed

## 本轮验收范围

- 视觉基准：`design/reference-mobile.png`（825 × 1906），沿用原样稿的品牌红、中文系统字体、移动端卡片层级和真实周麻婆 Logo。
- 数据基准：2026-08-06 18:06:50 成功快照，130 家门店、13 个区域；未修改业务金额。
- 核心改动：底部总表防误触、反超上一名金额、30 分钟排名变化、冠军状态、紧凑选择区、奖金提示，以及“累计”对比文案统一改为“总进度”。
- 实测视口：393 × 852 与 370 × 852，像素密度按浏览器真实 CSS 像素检查；两种宽度均无横向滚动。

## 状态截图与对照证据

- 总部普通状态：`design/v1.1-hq-393x852.png`。
- 区域冠军状态：`design/v1.1-region-champion-393x852.png`。
- 门店上涨状态：`design/v1.1-store-rising-393x852.png`。
- 门店下降状态：`design/v1.1-store-falling-393x852.png`。
- 门店冠军状态：`design/v1.1-store-champion-393x852.png`。
- 窄屏冠军状态：`design/v1.1-store-champion-370x852.png`。
- 样稿与普通状态并排：`design/qa-comparison-v1.1-reference-vs-falling.png`。
- 总部／区域冠军／上涨／全国冠军四态并排：`design/qa-comparison-v1.1-four-states.png`。
- 冠军改造前后对照：`design/qa-comparison-v1.1-champion-before-after.png`。

全部截图均为页面真实渲染结果，无后期修改页面内容。字号、间距、冠军纹理和徽章在完整手机截图中均可直接判断，因此不需要额外的局部放大证据。

## 视觉检查

- 查看范围压缩为两级紧凑选择区，选择控件仍有清晰边框、标签和选中态；393px 下选择区高度约 120px，核心经营数据提前进入首屏。
- 冠军英雄区使用深品牌红渐变、香槟金细边、真实 Logo 水印和明确的“冠军”徽章；只保留一次性轻微高光，不使用撒花、闪烁或循环动画。
- 普通状态沿用白底；上涨为小面积绿色提示，下降为柔和琥珀色提醒，状态辨识清楚且未抢夺核心金额。
- 冠军标签写明具体身份，例如“全国今日冠军”“全国区域今日冠军”，不会因今日／总进度切换而产生误解。
- 核心金额、排名、三档目标、奖金和反超差额在 393px 与 370px 下均无裁切、重叠或异常换行。
- 图标来自现有 Radix 图标库；冠军水印使用项目内真实品牌 Logo，没有占位图、手绘 SVG、Emoji 或 CSS 图形替代。

## 内容与数据检查

- 门店显示全国榜反超差额，区域显示全国区域榜反超差额；计算严格达到上一名达成率后再加 ¥1。
- 第一名显示“当前领先，守住第1”；目标为零、排名缺失或不可计算时不虚构金额。
- 排名变化分别按今日／总进度和门店／区域计算；没有可比快照时显示“等待下一次快照”。
- 排名对比文案已统一为“今日比总进度领先／落后 X 名”或“今日与总进度排名持平”。
- 当前工作簿只有已获得奖金，没有官方固定“完成 100% 可得总奖金”；页面因此可靠展示“已获得奖金＋冲刺挑战目标还差多少”，不估算未来奖金。技术中心提供可选 `bonusGoal` 后，页面才显示“总计可拿／还可增加”。
- `Store.rankChange30` 继续保留兼容；新增今日／总进度排名变化字段均为可选字段，不影响日华现有版本读取。

## 交互检查

- 底部“全部门店／区域门店”行已改为纯浏览，点击不跳转；搜索、排序和展开仍可使用。
- 顶部两级选择器、冲刺榜、关注榜和区域排名仍可进入相应详情。
- 总部只需选择一级范围；进入区域后才显示区域总览或门店的第二级选择，本机记忆上次范围与口径。
- 今日／总进度切换、门店与区域选择、排行榜展开和搜索均正常。
- 开启“减少动态效果”时，冠军高光和入场动画完全关闭。

## 自动与运行检查

- `npm run verify`：通过，包括运行时保护、130 家门店／13 个区域数据校验、规则测试、排名测试、TypeScript 和正式构建。
- `npm run test:sites`：4 项全部通过。
- Playwright 独立运行环境缺少其专用 Chromium 二进制，因此未额外下载浏览器；已按用户选择改用应用内浏览器完成相同核心交互与响应式检查。
- 应用内浏览器控制台错误和未处理运行时异常：0。

## 修正记录

1. P2 冠军辨识度不足：最初仅使用浅色背景，冠军状态不够明确；改为深红／香槟金英雄区、真实 Logo 水印和明确冠军徽章。
2. P2 首屏被筛选区占用：最初按钮和选择框偏高；压缩为约 120px 的两级选择区，文字清晰度不降低。
3. P2 总表误触：最初整行可进入详情；只取消底部总表行跳转，保留顶部快捷入口。
4. P2 奖金口径风险：工作簿无法可靠推算固定总奖金；删除推测性表达，改用已获得奖金与挑战目标差额，并预留官方可选字段。
5. P2 经营文案：将“今日比累计……”统一改为“今日比总进度……”，与门店使用者理解一致。

## 已知且符合预期

- 30 分钟排名变化必须存在最接近 30 分钟、误差不超过 10 分钟的成功快照；否则显示等待，不伪造变化。
- 页面关闭普通文本选择、右键和导出入口，但公开网页无法真正阻止截图、开发者工具读取或数据转存。
